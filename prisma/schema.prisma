generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum Role {
  ADMIN
  GUIDE
  TOURIST
}

model User {
  userId     String  @id @default(cuid())
  name       String
  email      String  @unique
  photo      String? @default("https://avatar.iran.liara.run/public/10")
  firebaseId String
  role       Role    @default(TOURIST)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  Guide     Guide?

  @@map("users")
}

model Guide {
  guideId  String   @id @default(cuid())
  language String[]
  skills   String[]
  bio      String
  address  String
  phone    String

  userId String @unique
  user   User   @relation(fields: [userId], references: [userId])

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
